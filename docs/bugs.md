calendar, when i choose weekly, it says That recurrence pattern isn't supported.

when i click start quiz:
+page.svelte:17
POST http://localhost:5173/api/quiz/runs 500 (Internal Server Error)
start @ +page.svelte:17

The back arrow button for each page are very inconsistent

The top tab navigation seems not to be the best idea, first this is that it doesnt scroll nor wrap with the screen so users could not really see all the badge tabs, second problem is that it does not feel natrual to navigate with these tabs, maybe because this doesn't feel like a navigation button, or maybe because the tabs does not persist between pages, i dont know but it is bad. reasearch online for good mobile ui designs, apply miller's law when fixing your ui
