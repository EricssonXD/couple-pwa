// Re-export Better-Auth-generated tables (run `bun run auth:schema` to regenerate).
export * from './auth.schema';

// DuoSync app schema will be added here in Phase 1 (couple, link_code,
// location_ping, message, geo_moment, place, push_subscription, mood_log,
// daily_question). See plan.md §3.
